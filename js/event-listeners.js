
//let simulatorUIContainer = document.querySelector('.simulator-ui-container');

//const handleLoad = (event)=>{
    // pull the data out of the custom event...
    //const data = event.detail.data;
    // and create a new todo for each object
    //data.forEach( todo => {
      //event.target.append( createTodo(todo) )
    //})
  //}
  
  // finally, we wire in our custom event!
  //simulatorUIContainer.addEventListener("todo.load", handleLoad);